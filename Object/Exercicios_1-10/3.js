// 3. Função que mostra o comprimento de um objeto.
function lengthObjeto() {
  var student = {
    name: "David Rayy",
    sclass: "VI",
    rollno: 12,
  };
  console.log(Object.keys(student).length);
}
lengthObjeto();

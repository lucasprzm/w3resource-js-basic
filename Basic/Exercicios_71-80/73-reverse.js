// 73.Programa para reverter os elementos de um array de 3 elementos.
function reverse(array) {
  if (array.length == 3) {
    console.log(array.reverse());
  }
}
reverse([1, 2, 3]);
reverse([4, 5, 6]);
